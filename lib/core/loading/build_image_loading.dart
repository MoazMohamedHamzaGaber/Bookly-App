import 'package:flutter/material.dart';

Widget buildImage() {
  return Container(
    width: 75,
    height: 120,
    decoration: BoxDecoration(
      color: Colors.black,
      borderRadius: BorderRadius.circular(16),
    ),
  );
}